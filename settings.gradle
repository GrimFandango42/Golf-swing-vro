pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://maven.google.com' }
        // Repository for AICore SDK
        maven { url 'https://developer.android.com/studio/build/maven-central' }
        // Repository for MediaPipe
        maven { url 'https://storage.googleapis.com/mediapipe-assets/maven' }
    }
}
rootProject.name = "Golf Swing VRO"
include ':app'